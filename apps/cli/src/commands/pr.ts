import chalk from 'chalk';

export async function pr(repoUrl: string): Promise<void> {
  console.log(chalk.yellow(`⏳ Checking ${repoUrl} for stale docs...`));
  console.log(chalk.red('Not implemented yet — coming in Phase 6'));
}
