# Monorepo Example

Run:
```bash
ghost-docs explain ./examples/monorepo
```

Expected output: Monorepo detection, multiple packages/apps, shared dependencies.

```bash
ghost-docs reason ./examples/monorepo "What is the project type?"
# → Monorepo project
```
