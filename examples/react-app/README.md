# React App Example

Run:
```bash
ghost-docs explain ./examples/react-app
```

Expected output: Project summary, tech stack with React, entry points at `src/index.tsx`, folder responsibilities for `src/`, `public/`, etc.

```bash
ghost-docs reason ./examples/react-app "What is the entry point?"
# → src/index.tsx (main)
```
