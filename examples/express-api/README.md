# Express API Example

Run:
```bash
ghost-docs explain ./examples/express-api
```

Expected output: REST API via Express, entry points at `src/app.js` or `src/server.js`, authentication middleware detected, test files in `__tests__/`.

```bash
ghost-docs reason ./examples/express-api "Where are REST APIs?"
# → REST via Express at /
```
