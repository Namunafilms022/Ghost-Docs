{
  "name": "ghost-docs",
  "version": "0.1.0-dev",
  "private": true,
  "type": "module",
  "description": "AI Technical Writer that understands your codebase",
  "scripts": {
    "dev": "pnpm --filter @ghost-docs/cli dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.json",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
